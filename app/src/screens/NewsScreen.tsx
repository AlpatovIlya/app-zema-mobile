import React, {FC} from 'react';
import {SafeAreaView, Text} from 'react-native';

const NewsScreen: FC = () => {
  return (
    <SafeAreaView>
      <Text>Новости</Text>
    </SafeAreaView>
  );
};

export default NewsScreen;
